#include "monty.h"
/**
 * f_stack - prints the the top prints
 * @head: stack pile head
 * @counter: line_number
 * Return: no 0 return
*/
void f_stack(stack_t **head, unsigned int counter)
{
(void)head;
(void)counter;
bur.lffiy = 0;
}
